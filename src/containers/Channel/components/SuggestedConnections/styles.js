import createStyles from '@mui/styles/createStyles'
import makeStyles from '@mui/styles/makeStyles'

export default makeStyles(() =>
  createStyles({
    suggestedCompaniesResult: {
      alignItems: 'center',
    },
    suggestedCompaniesPagination: {
      width: '100%',
    },
  })
)
