import styled from '@emotion/styled'

export const CompanyName = styled('span')({
  textDecoration: 'underline',
  cursor: 'pointer',
})

export const AlertButtonsContainer = styled('span')({
  display: 'flex',
  alignItems: 'center',
  gap: '14px',
})
